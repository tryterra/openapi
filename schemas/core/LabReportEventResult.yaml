$schema: https://json-schema.org/draft/2020-12/schema
type: object
properties:
  source:
    $ref: LabReportEventResultSource.yaml
required:
  - source
allOf:
  - $ref: LabReportResult.yaml
description: One layered webhook result. Byte-identical to the retrieve result — same source, biomarker, measurement, interpretation and reference_ranges.
