$schema: https://json-schema.org/draft/2020-12/schema
type: object
properties:
  stroke_type:
    anyOf:
      - $ref: SwimStrokeType.yaml
      - type: "null"
  drill_type:
    anyOf:
      - $ref: DrillType.yaml
      - type: "null"
  equipment:
    anyOf:
      - type: array
        items:
          $ref: SwimEquipmentType.yaml
      - type: "null"
description: Swimming details for a step
