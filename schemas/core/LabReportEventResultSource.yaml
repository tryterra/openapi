$schema: https://json-schema.org/draft/2020-12/schema
type: object
properties: {}
allOf:
  - $ref: LabReportResultSource.yaml
description: The webhook source layer. Identical to the retrieve source layer — per-result collection date/time and region_name moved onto the shared base type, so the two cannot drift. Retained as a named type because it is published in the SDK.
