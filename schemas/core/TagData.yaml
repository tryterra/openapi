$schema: https://json-schema.org/draft/2020-12/schema
type: object
properties:
  tags:
    anyOf:
      - type: array
        items:
          $ref: TagEntry.yaml
      - type: "null"
    description: Array of user-entered tags for the day.
